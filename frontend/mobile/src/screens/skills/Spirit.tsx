import React, { useEffect, useRef } from "react";
import {
  View,
  Text,
  StyleSheet,
  ScrollView,
  Animated,
  Image,
} from "react-native";

const SpiritScreen: React.FC = () => {
  return <View></View>;
};

export default SpiritScreen;
