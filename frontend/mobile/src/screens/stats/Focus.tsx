import React, { useEffect, useRef } from "react";
import {
  View,
  Text,
  StyleSheet,
  ScrollView,
  Animated,
  Image,
} from "react-native";

const FocusScreen: React.FC = () => {
  return <View></View>;
};

export default FocusScreen;
